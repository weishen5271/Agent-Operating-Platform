# 数据库配置
database_url = "postgresql+psycopg://username:password@127.0.0.1:5432/agent_platform"

# LLM 配置 (可选)
# llm_base_url = "https://api.openai.com/v1"
# llm_api_key = "your-api-key"
# llm_model = "gpt-4"
# llm_temperature = 0.2

# 本地联调业务包上游示例服务时可显式放行 loopback；生产环境不要配置 127.0.0.1。
# http_executor_allowlist = ["127.0.0.1"]
